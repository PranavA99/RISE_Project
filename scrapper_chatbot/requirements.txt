# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Settings / env
pydantic-settings==2.2.1

# HTTP client (async scraping)
httpx==0.27.0

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.2.1          # faster parser for BeautifulSoup
Scrapy==2.15.0       # for more complex processes

# OpenAI (embeddings + chat)
openai==1.30.1

# Vector database
chromadb==0.5.0

# PDF export
fpdf2==2.7.9

# Dev / testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx                # also used as the test client for FastAPI
